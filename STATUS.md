# סטטוס פרויקט — Maayan Nails

עדכון אחרון: המערכת חיה בפרודקשן ופועלת. הקוד עובר `typecheck`, `lint` ו-25/25 בדיקות יחידה ללא שגיאות.

## ✅ חי בפרודקשן

- **האתר:** https://maayan-nails-livid.vercel.app (Vercel, מחובר ל-GitHub, דיפלוי אוטומטי בכל push ל-`main`).
- **GitHub:** Repository ציבורי `talyemini5-hub/maayan-nails`, כל הקוד מעודכן.
- **Supabase:** פרויקט חי `maayan-nails` (eu-central-1) — כל המיגרציות רצות, RLS מלא, נתוני זרע נטענו, בדיקות מניעת-תורים-כפולים (`booking_rules.sql`) עברו במלואן מול המסד החי.
- **משתני סביבה ב-Vercel:** כל מפתחות Supabase מוגדרים; `NEXT_PUBLIC_SITE_URL` מתוקן לכתובת האמיתית (במקום placeholder של localhost).
- **הגדרות Auth ב-Supabase:** Site URL ו-Redirect URLs עודכנו לכתובת האמיתית — קישורי התחברות במייל כבר לא שוברים.

## ✅ בנוי במלואו כ-UI

- אשף קביעת תור מלא (טיפול → תוספות → תאריך → שעה → פרטים → סיכום → אישור).
- התחברות ללא סיסמה (Magic Link + קוד בן 6 ספרות כחלופה לכניסה ממכשיר אחר) ו"התורים שלי" (קרובים/היסטוריה, ביטול, שינוי מועד עצמאי).
- גלריה ציבורית (Masonry, סינון, Lightbox).
- PWA מלאה (אייקונים, Service Worker, עמוד offline).
- אזור Admin מלא: דשבורד, ניהול תורים, יומן חודשי, שירותים ומחירים, גלריה, מודרציית ביקורות, לקוחות, הגדרות עסק ושעות פעילות.
- כל זה עובר typecheck, lint ובדיקות יחידה ללא שגיאות.

## 🔑 כניסה לאזור הניהול (למי שמפעילה את האתר)

1. גולשים ל-`/admin/login`, מזינים אימייל, מקבלים קישור/קוד.
2. **חד-פעמי:** ב-Supabase SQL Editor מריצים
   `update public.profiles set role = 'admin' where email = 'המייל שלך';`
3. נכנסים שוב ל-`/admin/login` → מגיעים לדשבורד המלא.

## ⛔ נותר לסיים (לא חוסם את תפקוד האתר)

- **הפעלת Resend / Custom SMTP ב-Supabase** — ממתין ל-`RESEND_API_KEY` ממך. ברגע שיסופק:
  - יופעלו מיילי אישור/עדכון תורים אוטומטיים ללקוחות.
  - יתאפשר לערוך את תבנית מייל ההתחברות ב-Supabase כך שהקוד בן 6 הספרות (לכניסה חוצת-מכשירים) יופיע במייל בפועל — כרגע הקוד נתמך בקוד אך לא מוצג במייל, כי עריכת התבנית דורשת SMTP מוגדר.
- **בדיקות E2E (Playwright)** מול המערכת הפרוסה — עדיין לא בוצעו.
