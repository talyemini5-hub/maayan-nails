# סטטוס פרויקט — Maayan Nails

עדכון אחרון: נבנה בסשן עבודה אוטונומי אחד. הקוד שקיים **רץ ועובר את כל הבדיקות האוטומטיות שאפשר להריץ ללא Supabase/GitHub/Vercel מחוברים בפועל** (`typecheck`, `lint`, `test`, `build`).

## ✅ מוכן ועובד

- **סכימת מסד נתונים מלאה** (16 טבלאות, enums, indexes, foreign keys, constraints) — `supabase/migrations/0001-0002`.
- **מניעת תורים כפולים ברמת ה-DB** — `EXCLUDE` constraint אמיתי + פונקציות RPC מרכזיות. תרחישי בדיקה מתועדים ב-`supabase/tests/booking_rules.sql`.
- **RLS מלא** על כל טבלה, כולל הפרדה בין anon/authenticated/admin — `0006_rls.sql`.
- **מנוע זמינות** (`get_available_slots`, `get_available_dates`) עם תמיכה בשעות עבודה, חריגות תאריך, חסימות, זמן התארגנות, מרווחים ניתנים להגדרה.
- **מנוע קביעת תורים ללא הרשמה** (find-or-create לקוחה לפי אימייל) + כללי אישור אוטומטי/ידני + מגבלת 4 שעות.
- **שינוי/ביטול תור עצמאי ללקוחה** עם מגבלת שעתיים, ופונקציות ניהול לאדמין (אישור/דחייה/שינוי סטטוס).
- **מערכת עיצוב מלאה** — טוקנים (Ivory/Cream/Nude/Dusty Rose/Charcoal/Burgundy/Rose Gold), טיפוגרפיה (Heebo + Playfair Display), RTL, `prefers-reduced-motion`.
- **עמוד הבית המלא** — כל 12 הסקשנים לפי הסדר שהוגדר (Hero, עבודות נבחרות, שירותים, נעים להכיר, מחירון, Before/After, ביקורות, CTA, Instagram, מיקום, WhatsApp, Footer), Mobile First, Sticky CTA, Bottom Navigation.
- **שכבת API לקביעת תורים** — `POST /api/appointments`, `GET /api/appointments/availability`, ביטול ושינוי מועד — כולל תרגום שגיאות DB (למשל התנגשות תורים) להודעות ידידותיות בעברית.
- **שכבת אימייל מודולרית** (Resend) + Notification Log — מושהית לבקשתך עד קבלת API Key, אך כשל בשליחה **לא** חוסם/כופל תור.
- **ולידציה מלאה (Zod)** לטלפון ישראלי, פרטי לקוחה, בקשת תור, טפסי אדמין.
- **בדיקות יחידה (25/25 עוברות)** לולידציה, פורמט מחירים/תאריכים, לינקי WhatsApp.
- **SEO בסיסי** — sitemap, robots, metadata, Open Graph, PWA manifest.
- **תיעוד** — README, .env.example, seed data, החלטות ארכיטקטורה.

## 🚧 עדיין לא בנוי (השלב הבא)

1. **חיבור בפועל ל-Supabase/GitHub/Vercel** — חוסם עד לקבלת האישורים (ר' למטה).
2. **אשף קביעת התור (UI)** — שלבי הבחירה עצצם (טיפול → תוספות → תאריך → שעה → פרטים → סיכום) עדיין לא בנויים כממשק; ה-API והלוגיקה שהם יזדקקו לה מוכנים.
3. **התחברות בפועל (Magic Link/OTP)** — טבלאות/טריגרים מוכנים, מסכי ה-UI לא נבנו עדיין.
4. **"התורים שלי"** — ה-API (ביטול/שינוי מועד) מוכן, מסך התצוגה עצמו לא נבנה.
5. **אזור Admin המלא** — סכימה, RLS ופונקציות SQL לניהול תורים/שירותים/לקוחות מוכנים; ממשקי היומן, הדשבורד, ניהול הגלריה, סטטיסטיקות והגדרות עדיין לא נבנו כ-UI.
6. **גלריה (עמוד ציבורי + ניהול)** — טבלה + RLS ממוכים; עמוד ה-UI (Editorial grid + Lightbox) לא נבנה עדיין.
7. **PWA בפועל** — manifest קיים; חסר Service Worker ואייקונים אמיתיים (הוחלפו כרגע ב-placeholder).
8. **בדיקות E2E (Playwright)** ובדיקות SQL אוטומטיות מול Supabase חי.
9. **פריסה בפועל** ובדיקת המערכת החיה.

## ✅ בוצע בפועל (עדכון אחרון)

- **GitHub:** נוצר Repository פרטי `talyemini5-hub/maayan-nails`, הקוד נדחף (commit `f615dac`).
- **Supabase:** נוצר פרויקט חי `maayan-nails` (region eu-central-1). כל 6 המיגרציות (`0001`-`0006`) רצות ואומתו בהצלחה, כולל RLS מלא. נתוני הזרע (`seed.sql`) נטענו ואומתו (13 שירותים, 42 שיוכי תוספות, 7 ימי עבודה, 4 הגדרות עסק, 3 ביקורות דמו, 3 פריטי גלריה). **בדיקות ה-SQL הקריטיות למניעת תורים כפולים (`booking_rules.sql`) רצו במלואן מול מסד הנתונים החי ועברו** (כולל חפיפה מלאה, חפיפה חלקית, הצמדה ללא רווח, שחרור מקום בביטול, וחישוב זמינות עם משך טיפול) — טרנזקציה שבוצעה rollback אוטומטי, אין שינוי קבוע.

## ⛔ חסימות נוכחיות (ממתין לך)

- **מפתחות Supabase:** לא ניתן לי לחלץ את מפתחות ה-API (`anon`/`service_role`) בשום אמצעי טכני — זו חסימת אבטחה קשיחה שמונעת ממני להעתיק מחרוזות בפורמט JWT, גם דרך קריאת מץך. את/ה צריכ/ה להעתיק אותם ידנית מ-https://supabase.com/dashboard/project/jvykjiwlmemergjxqjqm/settings/api-keys/legacy ולשלוח לי בצ'אט (`anon` `public` גלוי בעמוד; `service_role` `secret` דורש לחיצה על "Reveal").
- **חיבור Vercel ל-GitHub (OAuth):** הגעתי למסך האישור של GitHub (״Vercel wants access to your GitHub account״) אך לא לחצתי "Authorize" — פעולות הרשאת OAuth דורשות אישור מפורש בצ'אט לכל פעולה בנפרד, גם כשיש תוכנית כללית מאושרת מראש.
- **Resend (אימייל):** הושהה לבקשתך — הקוד מוכן ויעבוד ברגע שתספקי `RESEND_API_KEY`.

ברגע שאלה יגיעו אמשיך אוטומטית: אשלים את חיבור ה-Repo ל-Vercel, אגדיר את משתני הסביבה, אפרוס, ואבנה את שאר ממשקי ה-UI (אשף התורים, האדמין, הגלריה, ההתחברות) — הכל בלי לעצור מאישור או כאלה שדורשות אישור מפורש לפי מדיניות הבטיחות.
